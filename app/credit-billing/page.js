import Walkthrough from "./Walkthrough";

export const metadata = {
  title: "Credit & Billing — component walkthrough",
  description:
    "All 28 components of HERE's credit and billing surfaces, in every variant and state that matters, with the user flow each one belongs to and two flows wired to a real append-only ledger.",
};

export default function CreditBillingPage() {
  return <Walkthrough />;
}
